# Arquivo de exemplo de variáveis de ambiente
# Copie este arquivo para .env e preencha com suas credenciais reais
# NUNCA comite o arquivo .env no Git!

# URI de conexão do MongoDB
# Para MongoDB local:
MONGODB_URI=mongodb://localhost:27017/projeto_mvc
# Para MongoDB Atlas (substitua com sua string de conexão):
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/projeto_mvc?retryWrites=true&w=majority

# Porta do servidor (padrão: 3030)
PORT=3030

# Chave secreta para assinar cookies de sessão
# IMPORTANTE: Use uma string aleatória forte em produção!
# Pode gerar uma com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=minha_chave_secreta_super_dificil_troque_em_producao

# Ambiente (development ou production)
NODE_ENV=development
